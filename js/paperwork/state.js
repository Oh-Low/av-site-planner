/**
 * Paperwork state — domain lives in js/domain/paperwork.js.
 */
export {
  DEFAULT_GRID_SIZE_IN,
  createElement,
  createManualSheet,
  duplicateSheet,
  emptyIdentity,
  emptyPaperworkState,
  isIdentityField,
  normalizeElement,
  normalizeGrid,
  normalizeIdentity,
  normalizePaperworkState,
  normalizeSheet,
  normalizeTitleBlockLogo,
} from "../domain/paperwork.js";
